modules:
  macro:
    - key: dynamic-macro
      title: Dynamic Macro
      resource: main
      resolver:
        function: resolver
      # function: macro-function
      description: A dynamic macro with custom parameters

  confluence:globalSettings:
    - key: helloworld-hello-world-content-byline-item
      resource: main
      resolver:
        function: resolver
      title: helloworld

  function:
    - key: resolver
      handler: index.handler
    # - key: macro-function
    #   handler: macrofunc.run
  
resources:
  - key: main
    path: static/hello-world/build

app:
  id: ari:cloud:ecosystem::app/55052d31-4b6e-46bf-bf33-c6de5049b626

permissions:
  scopes:
    - storage:app
    - write:confluence-content
  external:
    fetch:
      backend:
        - "https://login.microsoft.com"
        - "https://graph.microsoft.com"
